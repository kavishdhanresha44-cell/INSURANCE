# Insurance Policy Status Predictor (Streamlit)
### Steps
1. Push these files to a GitHub repo (keep flat—no folders).
2. Deploy to Streamlit Cloud with main file `app.py`.
3. Upload `Insurance.csv` or similar dataset from the sidebar.

Tabs:
- **Model Lab:** Runs Decision Tree, Random Forest, GBRT; outputs confusion matrices, ROC curves, and metrics.
- **Feature Importance:** Displays top 20 important features per model.

Nulls are automatically imputed before modeling.
